/* StudentLayout — placeholder, will be implemented later */
import React from 'react';
import { Outlet } from 'react-router-dom';

const StudentLayout = () => {
  return <Outlet />;
};

export default StudentLayout;
