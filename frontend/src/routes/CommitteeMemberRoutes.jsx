/* CommitteeMemberRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const CommitteeMemberRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Committee Member Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default CommitteeMemberRoutes;
