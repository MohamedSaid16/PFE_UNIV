/* DelegateRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const DelegateRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Delegate Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default DelegateRoutes;
