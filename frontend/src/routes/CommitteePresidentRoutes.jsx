/* CommitteePresidentRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const CommitteePresidentRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Committee President Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default CommitteePresidentRoutes;
