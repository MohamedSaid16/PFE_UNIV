/* TeacherRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const TeacherRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Teacher Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default TeacherRoutes;
