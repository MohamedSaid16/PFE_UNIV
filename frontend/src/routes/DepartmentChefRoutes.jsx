/* DepartmentChefRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const DepartmentChefRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Department Chef Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default DepartmentChefRoutes;
