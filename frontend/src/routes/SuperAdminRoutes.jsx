/* SuperAdminRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const SuperAdminRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Super Admin Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default SuperAdminRoutes;
