/* SpecialiteChefRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const SpecialiteChefRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Specialité Chef Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default SpecialiteChefRoutes;
