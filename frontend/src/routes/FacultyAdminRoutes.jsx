/* FacultyAdminRoutes — placeholder, will be implemented later */
import React from 'react';
import { Routes, Route } from 'react-router-dom';

const FacultyAdminRoutes = () => {
  return (
    <Routes>
      <Route index element={<div>Faculty Admin Dashboard — Coming Soon</div>} />
    </Routes>
  );
};

export default FacultyAdminRoutes;
